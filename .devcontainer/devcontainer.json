{
  "name": "MyStore Dev",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",
  "forwardPorts": [4200, 3000, 5000, 8000],
  "postCreateCommand": "cd backend && if [ -f package.json ]; then npm ci; fi && cd ../frontend && if [ -f package.json ]; then npm ci; fi",
  "postStartCommand": "echo 'Devcontainer ready. Run the VS Code task \"Start Dev Servers\" (Terminal → Run Task) to start frontend and backend.'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.vscode-typescript-next",
        "esbenp.prettier-vscode",
        "angular.ng-template"
      ]
    }
  },
  "remoteUser": "vscode"
}
